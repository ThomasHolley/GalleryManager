<?php

/**
 *
 * module/galerie/view/galerie/galerie/index.phtml
 *
 * This file has been generated with LaminasGen
 * https://github.com/ThomasLeconte/laminas-gen
 *
 */

$title = 'Mes galeries';
$this->headTitle($title);
?>

<h1><?= $this->escapeHtml($title) ?></h1>

<p>
    <a class="btn btn-outline-primary" href="<?= $this->url('galerie', ['action' => 'add']) ?>"><span class="glyphicon glyphicon-plus"></span><i class="fa-solid fa-plus"></i> Créer une galerie</a>
</p>

<table class="table">
    <tr>
        <th>Nom</th>
        <th>Description</th>
        <th></th>
    </tr>
    <?php if(count($galeries) > 0){ ?>
        <?php foreach ($galeries as $galerie) : ?>
            <tr>
                <td><a href="<?= $this->url('galerie', ['action' => 'detail', 'id' => $galerie->getId()]) ?>"><?= $this->escapeHtml($galerie->getNom()) ?></a></td>
                <td><?=$galerie->getDescription()?></td>
                <td>
                    <a href="<?= $this->url('galerie', ['action' => 'edit', 'id' => $galerie->getId()]) ?>" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i></a>
                    <a href="<?= $this->url('galerie', ['action' => 'delete', 'id' => $galerie->getId()]) ?>" class="btn btn-danger"><i class="fa-solid fa-trash"></i></a>
                </td>
            </tr>
        <?php endforeach; ?>
    <?php } else { ?>
        <tr>
            <td colspan="5">
                <div class="alert alert-info" role="alert">
                    Aucune galerie enregistrée ...
                </div>
            </td>
        </tr>
    <?php } ?>

</table>